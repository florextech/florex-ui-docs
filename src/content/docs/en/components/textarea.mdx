---
title: Textarea
---

import { Preview } from '@components/Preview';
import { Textarea } from '@florexlabs/ui';

<Preview client:load>
  <div style={{width:'100%',maxWidth:'320px'}}>
    <Textarea placeholder="Write your message..." rows={3} />
  </div>
</Preview>

> **[View in Storybook →](https://florextech.github.io/florex-ui/?path=/story/components-textarea)** — Interactive preview with controls

# Textarea

A styled multi-line text input.

## Import

```tsx
import { Textarea } from '@florex/ui';
```

## Usage

```tsx
<Textarea placeholder="Write your message..." />
```

## Props

Accepts all native `<textarea>` HTML attributes.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| placeholder | `string` | — | Placeholder text |
| disabled | `boolean` | `false` | Disables the textarea |
| className | `string` | — | Additional CSS classes |

## Examples

### With label

```tsx
<Label htmlFor="bio">Bio</Label>
<Textarea id="bio" placeholder="Tell us about yourself..." rows={4} />
```

### Disabled

```tsx
<Textarea placeholder="Read-only" disabled />
```
