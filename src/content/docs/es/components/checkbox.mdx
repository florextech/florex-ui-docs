---
title: Checkbox
---

import { Preview } from '@components/Preview';
import { Checkbox } from '@florexlabs/ui';

<Preview client:load>
  <Checkbox id="c1" label="Accept terms and conditions" />
  <Checkbox id="c2" label="Subscribe to newsletter" defaultChecked />
</Preview>

> **[View in Storybook →](https://florextech.github.io/florex-ui/?path=/story/components-checkbox)** — Interactive preview with controls

# Checkbox

A styled checkbox input with optional label.

## Import

```tsx
import { Checkbox } from '@florex/ui';
```

## Usage

```tsx
<Checkbox label="Accept terms" />
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| label | `string` | — | Text label displayed next to the checkbox |
| id | `string` | — | HTML id (links label to input) |
| className | `string` | — | Additional CSS classes |

Also accepts all native `<input>` HTML attributes (except `type`).

## Examples

### Controlled

```tsx
const [checked, setChecked] = useState(false);

<Checkbox
  label="Enable notifications"
  checked={checked}
  onChange={(e) => setChecked(e.target.checked)}
/>
```

### Disabled

```tsx
<Checkbox label="Read-only option" disabled checked />
```
