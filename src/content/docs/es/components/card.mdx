---
title: Card
---

import { Preview } from '@components/Preview';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@florexlabs/ui';

<Preview client:load>
  <Card style={{ width: "100%", maxWidth: "360px" }}>
    <CardHeader>
      <CardTitle>Card Title</CardTitle>
      <CardDescription>Card description text goes here.</CardDescription>
    </CardHeader>
    <CardContent>
      <p style={{ fontSize: "0.875rem", color: "#b8c4ba" }}>Card body content.</p>
    </CardContent>
  </Card>
</Preview>

## Import

```tsx
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@florexlabs/ui";
```

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `variant` | `"default" \| "muted" \| "hover"` | `"default"` | Card style |
| `padding` | `"none" \| "sm" \| "md" \| "lg"` | `"md"` | Padding size |
| `className` | `string` | — | Additional classes |
