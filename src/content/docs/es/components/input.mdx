---
title: Input
---

import { Preview } from '@components/Preview';
import { StorybookLink } from '@components/StorybookLink';
import { Input, Label } from '@florexlabs/ui';

<Preview client:load>
  <div style={{ width: "100%", maxWidth: "320px", display: "flex", flexDirection: "column", gap: "0.25rem" }}>
    <Label>Email</Label>
    <Input placeholder="you@example.com" type="email" />
  </div>
</Preview>

<StorybookLink component="Input" client:load />

## Import

```tsx
import { Input } from "@florexlabs/ui";
```

## Props

| Prop | Type | Default | Description |
|---|---|---|---|
| `placeholder` | `string` | — | Placeholder text |
| `type` | `string` | `"text"` | Input type |
| `disabled` | `boolean` | `false` | Disabled state |
| `className` | `string` | — | Additional classes |
